Spring:
  application:
    name: Config-dec

eureka:
  instance:
    prefer-ip-addres: true
  client:
    fetch-registry: true
    regidter-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka

